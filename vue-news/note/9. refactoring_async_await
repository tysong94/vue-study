# async await Vs. Promise

1. 사용 방법
- Promise
  axios.get()
    .then()
    .catch();
- async await
  var response = axios.get();

2. 예외처리
- Promise는 api 요청에 대해서만 실패했을 경우 catch 처리.
- async await는 try {} 내의 모든 javascript에 대해서 catch 처리.

3. axios.get의 결과값은 new Promise() 이다.

4. 사용법
- Promise를 반환하는 함수나 api 앞에 await를 붙임.

